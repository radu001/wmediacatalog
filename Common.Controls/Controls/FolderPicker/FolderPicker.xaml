<UserControl x:Class="Common.Controls.Controls.FolderPicker.FolderPicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" x:Name="folderPicker"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid x:Name="LayoutRoot" Height="199" Width="295">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TreeView x:Name="FoldersTree" Grid.Row="0"/>
        <ComboBox x:Name="DriveComboBox" ItemsSource="{Binding ElementName=folderPicker,Path=DriveList}" Grid.Row="1"/>
    </Grid>
</UserControl>
